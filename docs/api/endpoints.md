# Endpoint reference

## `GET /api/market`

Returns:

- `symbol`, `currency`, `mode`, `source` and `generatedAt`
- `latest` close, change, range and volume
- `forecast` next close, slope, model fit, residual error and points used
- `candles`, up to 60 normalized daily OHLCV observations
