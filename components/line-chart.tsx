"use client";
import type { Candle } from "@/lib/types";
const WIDTH=900,HEIGHT=310,PX=32,PY=28;
export function LineChart({candles,forecast}:{candles:Candle[];forecast:number}){if(candles.length<2)return <div className="chart-empty">Not enough observations.</div>;const plotted=candles.slice(-45),values=plotted.map(c=>c.close),min=Math.min(...values,forecast),max=Math.max(...values,forecast),range=Math.max(.01,max-min),cw=WIDTH-PX*2,ch=HEIGHT-PY*2;const point=(v:number,i:number,len=plotted.length)=>({x:PX+(i/Math.max(1,len-1))*cw,y:PY+((max-v)/range)*ch});const points=values.map(point),path=points.map((p,i)=>`${i===0?"M":"L"}${p.x},${p.y}`).join(" "),area=`${path} L${points.at(-1)!.x},${HEIGHT-PY} L${points[0].x},${HEIGHT-PY} Z`,last=points.at(-1)!,proj={x:WIDTH-4,y:point(forecast,plotted.length).y};return <div className="chart-wrap" role="img" aria-label="Historical closing price with one-step projection"><svg viewBox={`0 0 ${WIDTH} ${HEIGHT}`} preserveAspectRatio="none" className="chart-svg"><defs><linearGradient id="areaGradient" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="currentColor" stopOpacity=".25"/><stop offset="100%" stopColor="currentColor" stopOpacity="0"/></linearGradient></defs>{[0,.25,.5,.75,1].map(f=><line key={f} x1={PX} y1={PY+ch*f} x2={WIDTH-PX} y2={PY+ch*f} className="chart-grid"/>)}<path d={area} fill="url(#areaGradient)" className="chart-area"/><path d={path} className="chart-line"/><line x1={last.x} y1={last.y} x2={proj.x} y2={proj.y} className="chart-projection"/><circle cx={last.x} cy={last.y} r="5" className="chart-dot"/><circle cx={proj.x} cy={proj.y} r="5" className="chart-forecast-dot"/></svg><div className="chart-axis"><span>{format(plotted[0].date)}</span><span>{format(plotted.at(-1)!.date)}</span></div></div>}
function format(v:string){return new Intl.DateTimeFormat("en",{month:"short",day:"numeric"}).format(new Date(`${v}T00:00:00Z`));}
